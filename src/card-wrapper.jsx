

const index = ({children}) => {
    return(
        <div className="flex flex-wrap gap-[40px] w-full items-center justify-between mt-4">
            {children}
        </div>
    )
} 

export default index;